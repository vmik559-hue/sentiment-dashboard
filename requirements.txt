# Sentiment Pipeline - Vercel Deployment (Lightweight)

# Core
flask>=2.0.0
pandas>=1.3.0
numpy>=1.20.0
openpyxl>=3.0.0

# Web scraping
beautifulsoup4>=4.9.0
requests>=2.28.0

# NLP - Basic (always available fallback)
textblob>=0.15.0

# PDF extraction (optional, for local analysis only)
# PyMuPDF>=1.23.0
# pdfplumber>=0.10.0
# PyPDF2>=3.0.0

# Server (not needed on Vercel, used for Railway/Render)
# gunicorn>=21.0.0

# NLP - FinBERT (optional, adds ~2GB to image)
# Uncomment below for FinBERT support (uses TextBlob fallback if not installed):
# --extra-index-url https://download.pytorch.org/whl/cpu
# transformers>=4.30.0
# torch>=2.0.0
