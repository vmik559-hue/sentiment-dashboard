# Sentiment Pipeline - Railway Deployment
# Core
flask>=2.0.0
gunicorn>=21.0.0
pandas>=1.3.0
numpy>=1.20.0
openpyxl>=3.0.0

# Web scraping
beautifulsoup4>=4.9.0
requests>=2.28.0
curl-cffi>=0.7.0

# PDF extraction
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# NLP - Basic (fallback)
textblob>=0.15.0

# NLP - FinBERT (for production sentiment analysis)
# Note: These are large dependencies (~2GB total)
# Railway Pro can handle this
transformers>=4.30.0
torch>=2.0.0
